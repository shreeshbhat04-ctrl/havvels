# Haavvvels — ESP32 Classic specific defaults
# Enable software coexistence (Wi-Fi + BLE time-sharing)
CONFIG_ESP_COEX_SW_COEXIST_ENABLE=y
# BLE requires Bluetooth enabled
CONFIG_BT_ENABLED=y
CONFIG_BT_BLE_ENABLED=y
